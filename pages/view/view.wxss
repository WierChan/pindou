/* 作品查看：主体样式在 app.wxss 的 playpage 骨架里 */

/* 导入码弹窗 */
.code-big {
  margin: 6px 0 2px;
  padding: 14px 10px;
  background: #FFF7F0;
  border: 3px dashed var(--accent-dark);
  font-size: 26px;
  font-weight: 900;
  letter-spacing: 3px;
  text-align: center;
  color: var(--ink);
}
